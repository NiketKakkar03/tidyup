[package]
name = "tidyup-core"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
tidyup-platform = { path = "../tidyup-platform" }

[dev-dependencies]
tidyup-testkit = { path = "../tidyup-testkit" }

[lints]
workspace = true
